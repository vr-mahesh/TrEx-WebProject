import { ProfilePipePipe } from './profile-pipe.pipe';

describe('ProfilePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfilePipePipe();
    expect(pipe).toBeTruthy();
  });
});
